## 前提知識

- SQLとは?  
     → DBを操作する言語

- DBとは?  
    - データを整理して保存しておく仕組み  
    - 広義では図書館や辞書もDBと言える  
    - ジャンルやあいうえお順で整理されている  
    - 整理されて見つけやすくしたもの

- DBの種類  
    1. リレーショナル型
    2. ネットワーク型-IDMS
    3. オブジェクト型-ObjectStore
    4. NoSQL-MongoDB  
        etc...

- RDB  
    □　リレーショナル = 関係  
    データ同士の関係を表で表現する  

- DBMS
    - SQLの受付処理
    - 同時実行性
    - 暗号化  
    ※図書館の管理システムみたいなイメージ

- RDBMS 
    1. MySQL
    2. PostgreSQL
    3. OracleDB
    4. SQLserver

## RDBについて

- DBを考えよう  
    □　誰かに手伝ってもらう?
    - CLの社員表を作る  
    - コンビニの商品表を作る  

- RDBの詳細  
    表で管理するとは?  
        →Excelを想像してもらう。
    - 表=テーブル=エンティティ  
    - 行列とは  
    - 行 = レコード = 組 = タプル = row  
    - 列 = カラム = アトリビュート = 属性 = フィールド  

- SELECT文をとりあえず覚える  
    □　構文 = 言葉を組み立てる構造やルール
    ```sql
    -- 大文字,小文字どちらでもOK
    -- セミコロンは必須。忘れるな!!
    -- 文字列はシングルクォートで囲む
    SELECT カラム名, カンマで区切る AS エイリアス
    FROM テーブル名;
    WHERE カラム名 LIKE IN() HAVING
    ORDER BY ASK '昇順' DESK '降順'
    GROUP BY カラム名 LIMIT 1 <> IS NOT NULL
    BETWEEN AND
    -- ワイルドカード % 0以上 _ 1文字
    ```
    - https://sqlab.net/works/basic/
    1. SQL初級編
    2. SQL中級編  
    をやる

### 正規化
□　RDBを知る上で重要な考え  

- なぜ必要か?  
    - 同じデータを持った状態 → 冗長  
        1. 管理が大変
        2. ムダな容量を使う

- 正規化をすると   
    一箇所の変更だけでOK  
    → 管理が楽

- 正規化には段階がある
    1. 第1正規形
    2. 第2正規形
    3. 第3正規形
    4. ボイスコッド,推移完全従属 

- ↑ は覚えなくていい ↓ だけ覚えて

    1. 横方向を正規化  
        - 複数の注文が横並びに[名,価,数]がいくつもある
        - 横ではなく縦に伸びるようにする  

    2. 縦方向を正規化
        - 同じ人が何度も出てくる

- キーについて

| 主キー   | PK   | レコードを一意に識別するキー       |
|----------|------|------------------------------------|
| 外部キー | FK   | 他のテーブルの主キーを参照するキー |
| 複合キー | CK   | 複数の列を組み合わせて一意に識別するキー |  
    ※ CKは中間テーブルの重複登録を防ぐとき等で使う

- 制約についてはどうする?

- テーブルの種類  
    - マスタ系
    - トランザクション系

- この世の事象を表で表現するのが大事  
    小川さんの教え

### ER図
- リレーション = 関係  
    3種類を覚えればいい

### リレーション（関係）

| 1 : 1 | 1件に対して1件だけ対応する | 社員 1-1 社員証|
|-------|----------------------------|----------------|
| 1 : n | 1件に対して複数件対応する  |  顧客 1-< 注文 |
| n : n | お互いに複数件対応する     |学生 >-< 授業|  
    ※  n : n の場合は中間テーブルが必要  
- カーディナリティの表記は軽くでいいかな

### トランザクション

□ 取引のような処理が途中で止まると困ってしまう  
    場合に処理全体をひとまとまりにすること

- ACID特性
    1. Atomicity ( 原子性 )  
    → すべて実行されるか、一つも実行されないか
    2. Consistency ( 一貫性 )  
    → DBの状態が常に一定のルールや制約を満たす
    3. Isolation ( 独立性 )  
    → 他のトランザクションの途中結果に影響されない
    4. Durability ( 永続性 )  
    → トランザクションの結果が保持され続ける

### SQLの分類

- CRUDの4種類

| Create | 挿入   | INSERT |
|--------|--------|--------|
| Read   | 読込   | SELECT |
| Update | 更新   | UPDATE |
| Delete | 削除   | DELETE |

    - DCL ( ユーザー )
    - DDL ( DB )
    - DML ( テーブル )
    - TCL ( トランザクション )

### 型について

- 型はあるという事だけ覚える  
    容量をムダにしないために超重要だが  
    設計する人じゃないと関係ない

## DB構築してみる

1. PostgreSQL インストール
2. A5:SQL インストール
3. CMD で  DBの作成
4. CREATE TABLE
5. INSERT INTO
6. TRUNCATE and DROP TABLE
7. 時間があれば遊ぶ

## 関数について

| AVG   | 平均値を求める |
|-------|----------------|
| SUM   | 合計を求める   |
| COUNT | 行の数を求める |
| MAX   | 最高値を求める |
| MIN   | 最低値を求める |

## サブクエリ ( 副問合せ ) とは  
サブクエリとはクエリの中にクエリを書くこと

```sql
SELECT *
FROM users
WHERE age > (
            SELECT AVG(age) 
            FROM users
);
```

- 相関サブクエリってのもあるけど雰囲気だけ  
これは自分の部署の平均年齢より高いユーザーを調べる

```sql
SELECT *
FROM users u1
WHERE age > (
    SELECT AVG(u2.age)
    FROM users u2
    WHERE u2.department_id = u1.department_id
);
```

## ストアドについて
複数の SQL を一連の流れとしてDBMS側に登録しておける  
- メリット  
    1. AP ⇔ DB 間の通信を減らせる
    2. 同じ処理を再利用しやすい

- ストアドプロシージャ  
→ 複数のSQLや処理をまとめたもの

- ストアドファンクション
→ PGMのように値を返す独自関数が組める

補足 :  
    Orcacleだと登録する前のが PL/SQL  
    PostgreSQLだと PL/pgSQL って言うらしい

## 残りをやる

- https://sqlab.net/works/
- https://www.sqlzoo.net/wiki/SQL_Tutorial/ja  
    → 初歩の記法が難しい人用

# これ以降は話さない

## 3層スキーマ
|  外部  | ビュー     | UI側     |
|--------|------------|----------|
|  概念  | テーブル   | どの形式 |
|  内部  | 物理配置   | どこに   |

本日は、お時間いただきありがとうございます。

これまでに私が進めていたAIの活用検討での報告をさせていただきます。

今回お話させていただく内容としては、
検証条件
詳細
それぞれの評価
見ていただけるようであれば、
私がレビューした内容をお伝えできればと思います。

一応、前提と前回報告もまとめましたがツールのみ説明させていただきます。

ツールとしてはM365のエージェントとGitHubCopilotの2種類です。

以降はM365エージェントは通常Vopilotと表記しています。


検証条件として
まじ①が通常+Excelベースの仕様書の組み合わせ
②は手本にしてほしいPGM(ここでは教師PGMと記載)も渡す
③ExcelがAIには読みづらいようなのでMD形式に修正して渡す
④GitHubCopilotにMD形式の仕様書を渡すです。

それぞれの詳細は
①終えていない状態だったので結果使えない
②