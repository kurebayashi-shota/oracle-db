## 前提編

- SQLとは?  
     → DBを操作する言語

- DBとは?  
    - データを整理して保存しておく仕組み  
    - 広義では図書館や辞書もDBと言える  
    - ジャンルやあいうえお順で整理されている  
    - 整理されて見つけやすくしたもの

- DBの種類  
    1. リレーショナル型
    2. ネットワーク型-IDMS
    3. オブジェクト型-ObjectStore
    4. NoSQL-MongoDB

- RDB  
    ■リレーショナル = 関係  
    データ同士の関係を表で表現する  

- DBMS
    - SQLの受付処理
    - 同時実行性
    - 暗号化  
    ※図書館の管理システム

- RDBMS 
    1. MySQL
    2. PostgreSQL
    3. OracleDB
    4. SQLserver

## RDBについて

- DBを考えよう  
    誰かに手伝ってもらう?
    - CLの社員表を作る  
    - コンビニの商品表を作る  

- RDBの詳細  
    表で管理するとは?  
        →Excelを想像してもらう。
    - 表=テーブル=エンティティ  
    - 行列とは  
    - 行 = レコード = 組 = タプル = row  
    - 列 = カラム = アトリビュート = 属性 = フィールド  

- SELECT文をとりあえず覚える  
    構文 = 言葉を組み立てる構造やルール
    - https://sqlzoo.net/wiki/SELECT_basics/ja
    - https://sqlab.net/works/basic/

    ```sql
    -- 大文字,小文字どちらでもOK
    -- セミコロンは必須。忘れるな!!
    SELECT カラム名 FROM テーブル名;
    ```
    1. SQL初級編
    2. SQL中級編  
    をやる

### 正規化
RDBを知る上で重要な考え  

- なぜ必要か?  
    - 同じデータを持った状態 → 冗長  
        1. 管理が大変
        2. ムダな容量を使う

- 正規化をすると   
    一箇所の変更だけでOK  
    → 管理が楽

- 正規化には段階がある
    1. 第1正規形
    2. 第2正規形
    3. 第3正規形
    4. ボイスコッド,推移完全従属  
        →でもそんなのは覚えなくていい
    1. 横方向を正規化  
        - 複数の注文が横並びに[名,価,数]がいくつもある
        - 横ではなく縦に伸びるようにする  

    2. 縦方向を正規化
        - 同じ人が何度も出てくる

- 表の種類  
    - マスタ系
    - トランザクション系

## ER図
- 関係
    - リレーション=関係
    - 1=1,1=多
    - 多=多のみ中間テーブルほしい
    
- カーディナリティ

## トランザクション

- ACID特性
    1. 終わるかやるか
    2. 前後で辻褄が合う
    3. 互いに干渉しない
    4. データが失われない

## SQLの分類

- CRUDの4種類
    1. 挿入 create=insert
    2. 読込 read=select
    3. 更新 update=update
    4. 削除 delete=delete

    - DCL ( ユーザー )
    - DDL ( DB )
    - DML ( テーブル )
    - TCL ( トランザクション )

## 型について

- 型はあるという事だけ覚える  
    容量をムダにしないために超重要だが  
    設計する人じゃないと関係ない

# これ以降は話さない

## 3層スキーマ
     1. 外部 = ビュー = UI側でどう用意するか
     2. 概念 = テーブル = どの形式
     3. 内部 = 物理配置 = どこに